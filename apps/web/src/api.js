const csrf=()=>document.cookie.split('; ').find(row=>row.startsWith('aiopt_csrf='))?.split('=').slice(1).join('=')||'';
export async function api(path,options={}){const method=options.method||'GET',headers={...(options.body?{'Content-Type':'application/json'}:{}),...(method==='GET'?{}:{'X-CSRF-Token':decodeURIComponent(csrf())}),...options.headers};let response;try{response=await fetch(`/api/v1${path}`,{...options,headers,credentials:'same-origin'})}catch{throw new Error('The server is unavailable. Try again shortly.')};const payload=await response.json().catch(()=>({}));if(!response.ok){const detail=Array.isArray(payload.detail)?payload.detail.map(item=>item.msg).join('. '):payload.detail;const error=new Error(detail||'Request failed');error.status=response.status;throw error}return payload}
